# PHASE 31 — Honesty / positioning hardening  ✅  (V3 complete)

Makes the LANGUAGE as honest as the engine, per GPT-5.6's teardown: the danger is a novice
trusting a number as validated truth.

## Delivered
- `positioning.py` — one honest source of truth:
  - `CAPABILITIES` — explicit can / cannot, the honest promise ("run a disciplined,
    evidence-backed process and know where expert judgment is required") and the promise it will
    NOT make ("does not let anyone operate like a 20-year veteran or close without the reps").
  - `AUTHORITY_MATRIX` — research/calculate/draft/recommend allowed; send/submit/raise require
    human approval; **commit funds or wire = AI may NEVER do this**.
  - `STANDARD_DISCLAIMER` — advisory, source-gated, UNCALIBRATED-until-outcomes, route to counsel.
  - `assumptions_sheet()` — surfaces every driver + source-quality; unknowns stay "unknown",
    never silently defaulted.
- `tools/about_tools.py` — `capabilities` (honest what-it-does + authority matrix + positioning:
  "screen in seconds; verify as documents arrive; rents = direction+range, verify with lease
  comps; rates = benchmark, then executable quotes") and `deal_assumptions` (the assumption sheet).
  72 → 74 tools.

## Verified
2 new tests; full suite **612 passed**. Confirmed: guaranty tiering already worked
(field is `guaranty_tier`; earlier "gap" was a wrong probe key, not a bug).

## V3 status: COMPLETE
Truth (25-26) · Control + Nearby + Motivation (27-29) · Memory + Decision (30) ·
Master-Lease Arbitrage (32) · Honesty (31). 74 tools, 612 tests. Next: live deployment.
