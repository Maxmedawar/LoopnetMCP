# FRED fixture provenance

`series.json` (DGS10), `mortgage30us.json`, and `sofr.json` are fresh real
responses captured 2026-07-13 from FRED's official observations API using the
correct `api_key` query parameter. Responses were bounded to three recent
observations for deterministic tests. All FRED fixtures are live-confirmed.
