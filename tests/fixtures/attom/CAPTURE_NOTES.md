# ATTOM fixture notes

`detail_owner.json`, `sales_comparables.json`, and `avm.json` are representative
payloads built from ATTOM's documented v1 response fields.

**# VERIFY against a live paid response:** `CRE_ATTOM_API_KEY` is intentionally
not configured. ATTOM is pay-per-use and remains completely off until Max sets
that key explicitly. Replace these fixtures with sanitized live captures before
removing this marker.
