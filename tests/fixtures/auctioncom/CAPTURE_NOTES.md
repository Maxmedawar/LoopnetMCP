# Auction.com fixture

Captured 2026-07-13 from the live named GraphQL search operation at
`https://graph.auction.com/graphql`. The page's production operation used
`seek_listings_from_filters`; a reduced named query was replayed directly and
returned 439 active Texas results. This fixture contains two unaltered results.

The request and response shapes are live-confirmed.
