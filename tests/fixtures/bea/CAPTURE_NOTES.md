# BEA fixture provenance

`regional.json` is a fresh real response captured 2026-07-13 from the official
BEA Regional API for `CAGDP1`, line 1, Travis County (`GeoFIPS=48453`), and
`Year=LAST5`. The request succeeded with the configured BEA key. The echoed
request block was omitted so no credential enters the fixture. All BEA fixtures
are live-confirmed.
