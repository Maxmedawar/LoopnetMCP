# Regrid fixture notes

`parcel.json` is a representative GeoJSON response built from Regrid's public
v2 parcel schema and documented address endpoint.

**# VERIFY against a live paid response:** `CRE_REGRID_API_KEY` is intentionally
not configured. Regrid bills by returned parcel record and remains completely
off until Max sets that key explicitly. Replace this fixture with a sanitized
live capture before removing this marker.
